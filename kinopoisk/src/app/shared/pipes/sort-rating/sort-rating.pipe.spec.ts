import { SortRatingPipe } from './sort-rating.pipe';

describe('SortRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
