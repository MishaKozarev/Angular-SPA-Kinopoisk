import { ColorRatingDirective } from './color-rating.directive';

describe('ColorRatingDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorRatingDirective();
    expect(directive).toBeTruthy();
  });
});
